<div class="container">
  <h1>Job IDs</h1>
  <ul>
    <% @jobs.each do |job| %>
      <% unless job.new_records.empty? %>
        <li class='job_link'><%= link_to( job.job_id, job ) %> (<%= job.new_records.size %>) </li>
        <% end %>
    <% end %>
  </ul>
</div>

